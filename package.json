{
    "name": "Capstone-Project",
    "type": "module",
    "scripts": {
        "start": "echo 'No workspace start script needed'",
        "prebuild": "echo 'No workspace prebuild script needed'",
        "build": "echo 'No workspace build script needed'",
        "pretest": "echo 'No workspace pretest script needed'",
        "test": "echo 'No workspace test script needed'"
    },
    "engines": {
        "node": ">=16.0.0",
        "npm": ">=7.0.0"
    }
}
